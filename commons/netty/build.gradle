apply from: "${getRootDir()}/base.gradle"

group = "netty"

dependencies {

  compileOnly project(":commons:core")
  compileOnly project(":commons:pipeline")
  compileOnly 'io.netty:netty-all:4.1.34.Final'

}
