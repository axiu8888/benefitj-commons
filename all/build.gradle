jar {
  archivesBaseName = "benefitj-commons"
}

// 打包到一个项目
ext.allInOne = true
// 上传到nexus/maven仓库
ext.upload = true

dependencies {
  // compileOnly、implementation、api

  def deps = [
      ":core",
      ":eventbus",
      ":frameworks",
      ":device",
      ":netty",
      ":mqtt",
      ":javastruct",
      ":interpolator",
      ":pipeline",
      ":consistent-hash",
  ]
  if (allInOne) {
    deps.forEach{compileOnly(project(it)) }
  } else {
    deps.forEach{api(project(it)) }
  }
}



