apply from: "${scriptFile}"

// 上传到nexus/maven仓库
ext.upload = false

dependencies {
  [
      project(":core"),
      project(":frameworks"),
      "mysql:mysql-connector-java:$mysqlVersion",
  ].forEach {
    api(it)
  }
}

