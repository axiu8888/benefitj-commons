apply from: "${scriptFile}"

// 上传到nexus/maven仓库
ext.upload = false

dependencies {
  [
      project(":core"),
      project(":frameworks"),
      "com.mysql:mysql-connector-j:$mysqlVersion",
  ].forEach {
    api(it)
  }
}

